# Shader library

Reusable GLSL chunks — noise, easing, color, SDF — that demos compose from.

Wave 1 will populate this with adapted snippets from `basementstudio/shader-lab`.
Until then, demos import directly from `src/shaders/*.glsl`.
