'use client';

/**
 * Global R3F Canvas placeholder.
 *
 * Wave 1 will replace this with the View-tracked global canvas pattern
 * borrowed from `basementstudio/website-2k25` — one Canvas, many
 * `<View>` components that route 3D output to specific DOM positions.
 */

export function GlobalCanvas() {
  return null;
}
